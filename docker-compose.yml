version: '3.8'

services:
  pg:
    image: bitnami/postgresql:latest
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: ggest
      POSTGRES_PASSWORD: ggest
      POSTGRES_DB: ggest
